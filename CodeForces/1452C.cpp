#include <bits/stdc++.h>

using namespace std;

int t;
string str;

void solution() {
    cin >> str;

    for (int i = 0; i < str.length(); i++) {
        if ()
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin >> t;

    while (t--)
        solution();

    return 0;
}