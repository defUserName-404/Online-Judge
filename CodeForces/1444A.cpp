#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

ll t, p, q, x;

void solution()
{
    cin >> p >> q;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> t;

    while (t--)
        solution();

    return 0;
}