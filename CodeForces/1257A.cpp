#include <bits/stdc++.h>

using namespace std;

int t, n, x, a, b;

void solution() {
    cin >> n >> x >> a >> b;

    if (x == 0)
        cout << abs(a - b);

    else {
        for
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> t;

    while (t--)
        solution();

    return 0;
}