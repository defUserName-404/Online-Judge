#include <map>
#include <vector>

// TODO: Unsolved

// class Solution
// {
//   public:
//     double mincostToHireWorkers(std::vector<int> &quality, std::vector<int> &wage, int k);
// };

// double Solution::mincostToHireWorkers(std::vector<int> &quality, std::vector<int> &wage, int k)
// {
//     std::multimap<int, int, std::greater<>> mMap;

//     for (int i = 0; i < quality.size(); i++)
//         mMap.insert(std::make_pair(quality[i], wage[i]));

//     for (const auto it : mMap)
//     {

//     }
// }