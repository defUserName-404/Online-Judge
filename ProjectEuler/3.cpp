#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

bool isPrime(ll i) {
}

void solution() {
    ll n = 600851475143;
    ll ans;

    for (ll i = n; i >= sqrt(n); i--) {
        if
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solution();

    return 0;
}